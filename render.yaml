# Blueprint Render — deploy a partir do GitHub
# https://dashboard.render.com/blueprints
#
# URLs típicas após o deploy:
#   Site: https://barbearia-web.onrender.com
#   API:  https://barbearia-api.onrender.com

databases:
  - name: barbearia-db
    plan: free
    postgresMajorVersion: "16"

services:
  - type: web
    name: barbearia-api
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /v3/api-docs
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: barbearia-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRATION_MS
        value: "86400000"
      - key: CORS_ALLOWED_ORIGINS
        value: https://barbearia-web.onrender.com
      - key: UPLOAD_DIR
        value: /app/uploads
      - key: BACKUP_ENABLED
        value: "false"
      - key: OAUTH_DEV_MODE
        value: "true"

  - type: web
    name: barbearia-web
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./frontend/Dockerfile
    dockerContext: ./frontend
    envVars:
      # Render injeta env vars também como build-args do Docker
      - key: VITE_API_URL
        value: https://barbearia-api.onrender.com/api
